def delineate_watershed(dem_path, outlet_point):
    """
    Extract watershed boundary upstream of outlet point.
    """

    # load DEM
    # compute flow direction
    # compute flow accumulation
    # delineate basin

    return basin_polygon
